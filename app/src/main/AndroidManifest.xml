<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.appaccademy.speedymarkt">
    <uses-permission android:name="android.permission.INTERNET" ></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeedyMarkt">
        <activity android:name=".RicercaSupermercati" />
        <activity android:name=".Registrazione" />
        <activity android:name=".Profilo" />
        <activity android:name=".Carrello" />
        <activity android:name=".Accesso_admin" />
        <activity android:name=".Inserimento_prodotti" />
        <activity android:name=".Inserimento_attivita" />
            <activity android:name=".SchermataIniziale" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>